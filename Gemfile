source "https://rubygems.org"

# GitHub Pages runs Jekyll with this gem's pinned versions.
# Local builds with `bundle exec jekyll serve` will match production.
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on newer Ruby versions
gem "webrick"
